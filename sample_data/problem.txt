Write a Python function that sums all even numbers in a list.

Requirements:
- Function name: sum_evens(numbers)
- Handle empty lists
- Handle invalid input gracefully
- Include comments
- Show clear logic

Example:
  sum_evens([1, 2, 3, 4, 5, 6]) should return 12
  sum_evens([]) should return 0
  sum_evens([1, 3, 5]) should return 0
