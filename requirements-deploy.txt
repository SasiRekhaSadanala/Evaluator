# Core API framework
fastapi==0.100.0
uvicorn[standard]==0.23.2
pydantic==2.0.0
python-multipart==0.0.6

# Database
psycopg2-binary==2.9.9

# Auth
PyJWT==2.8.0

# Environment
python-dotenv==1.0.0

# HTTP client (for Groq + OpenRouter calls)
httpx==0.25.0

# File handling
aiofiles==23.2.1

# AST parsing (lightweight)
tree-sitter==0.21.3
tree-sitter-python==0.21.0
tree-sitter-cpp==0.22.0

# Numerical operations
numpy==1.24.4

# Async fix for FastAPI + sync wrappers
nest-asyncio==1.6.0

# NOT included (too heavy for Render free tier):
# torch
# transformers
# sentence-transformers
# llama-cpp-python
# google-generativeai  (not needed — using Groq + OpenRouter now)
